
name: Verify curated research repository

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify repository integrity and publication boundary
        run: python3 VERIFY_REPOSITORY.py
      - name: Verify curated Phase-32 evidence manifest
        run: python3 EVIDENCE/PHASE32_REFEREE_PACKET/VERIFY_BUNDLE.py
