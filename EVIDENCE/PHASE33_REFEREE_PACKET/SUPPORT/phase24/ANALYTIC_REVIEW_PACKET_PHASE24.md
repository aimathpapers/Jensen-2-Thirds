# Phase 24 analytic adversarial-review packet

Candidate source commit: `SELF` (resolve to the commit containing this file)
Review class: fresh separated AI pre-review unless a human reviewer is named
Embargo: confidential working mathematics; do not publish

## Instructions

Review the immutable candidate without reading prior review reports,
responses, or dispositions. Build independent scripts from the mathematical
definitions and primary sources, not from repository result artifacts. State
explicitly whether you are an AI or human reviewer; do not call AI-only work
peer review.

Return: a gate table, numbered findings with P0--P3 severity, independent-
recalculation record, unchecked-claims list, release recommendation R0--R3,
and conflict/separation statement.

## Gates

- **A0:** factor-eight Mellin identity and exact xi coefficient convention.
- **A1:** Lemma S Rouché comparison, branch patching, reality, and ordering of
  the `Q_N != 0` argument.
- **A2:** legal shifted contour, endpoint connectors, signed Gaussian error,
  and higher theta-mode suppression.
- **A3:** fixed nested-sector statement of Theorem 21B and proportional-disc
  Cauchy differentiation.
- **A4:** implicit saddle derivative constants `2,-2,4,-12,48`, including
  normalization and the `N=2x-2` chain rule.
- **A5:** exact sixth-order denominator and coefficientwise majorant, without
  treating numerical sampling as proof.
- **A6:** elementary cube-integral `C^1` estimate, paired gamma boundary, and
  the common parameter-derivative bounds.
- **A7:** contraction branch, exact box margins, local (not global)
  uniqueness, and threshold consistency.
- **A8:** paired complex polygamma segments and containment of the thickened
  interpolation domain.
- **A9:** complex Hermite--Genocchi/Newton remainder, its hypotheses, six
  nodes, and the exact `1/720` normalization.
- **A10:** exponent arithmetic from the sixth-order defect to
  `n^2 log(n+2) >= K d^3`, plus the effectivity disclosure.

## Mandatory independent recalculations

Recompute at least the factor-eight identity, Lemma S boundary inequality,
one complex contour point away from the real axis, the fifth/sixth derivative
constants through an independent path, the paired-polygamma scale, and the
Hermite--Genocchi normalization. Report anything not checked.

## Phase-24-specific changes

The candidate newly pins MMP v3/MSS sources, adds selected Lean finite
adapters, exact interval certificates, mutation gates, and a unified
manuscript. The full six-simplex identity is still a disclosed paper lemma;
do not infer it from the smaller Lean adapter.

The preceding freeze received a correlated AI re-review that informed this
repair revision. Those prior reports and their dispositions are intentionally
absent from this packet. Derive every gate afresh rather than trusting the
new regression suite.
